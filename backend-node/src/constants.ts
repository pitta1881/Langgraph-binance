/** Number of tickers shown in the heatmap grid. */
export const HEATMAP_SIZE = 20;

/** Number of tickers shown in the scrolling ticker banner. */
export const BANNER_SIZE = 10;

/** Default fetch timeout in milliseconds for Binance and CoinGecko calls. */
export const DEFAULT_FETCH_TIMEOUT_MS = 10_000;

/** Timeout in milliseconds for the Python agent (multi-step LLM pipeline). */
export const AGENT_FETCH_TIMEOUT_MS = 120_000;
